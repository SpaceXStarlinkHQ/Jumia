[phases.setup]
nixPkgs = ["nodejs_20"]
cmds = ["corepack enable"]

[phases.install]
cmds = ["corepack prepare pnpm@10.26.1 --activate && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["node artifacts/api-server/build.mjs"]

[start]
cmd = "node artifacts/api-server/dist/index.mjs"
