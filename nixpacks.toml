[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@10.26.1 --ignore-scripts && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["node artifacts/api-server/build.mjs"]

[start]
cmd = "node artifacts/api-server/dist/index.mjs"
